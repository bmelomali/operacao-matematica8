let numMulti;
let numMulti2;
let resultado;

numMulti = Number(prompt("adiciona o primeiro número"));
numMulti2 = Number(prompt("adiciona o segundo número"));
resultado = numMulti * numMulti2

alert(`resultado da multiplicação é ${resultado}`);